import { createTheme } from '@mui/material/styles';

const theme = createTheme({
	palette: {
		primary: {
			main: '#FF3838',
		},
		secondary: {
			main: '#575755',
		},
		success: {
			main: '#53AE71',
		},

		white: {
			main: '#FFFFFF',
		},
	},
});

export default theme;
